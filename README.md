# AI Resume Analyzer

## Structure

```bash
ai-resume-analyzer/
├── backend/    → Express API
└── frontend/   → Vite + React
```

## Local Development

```bash
# Backend
cd backend && npm install && npm run dev

# Frontend
cd frontend && npm install && npm run dev
```
